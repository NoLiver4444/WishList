server:
  port: :8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: wishlist_db
  sslmode: disable

jwt:
  secret: your-secret-key-change-in-production
  expiration: 24

cors:
  allowed_origins:
    - http://localhost:5173
    - http://localhost:3000